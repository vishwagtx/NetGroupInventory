name: .NET CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
      - master
  pull_request:
    branches:
      - main
      - develop
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        dotnet-version: [6.0.x, 8.0.x] # Change based on your .NET version

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ matrix.dotnet-version }}

      - name: Restore Dependencies
        run: dotnet restore MyWpfApp.sln

      - name: Build Project
        run: dotnet build MyWpfApp.sln --configuration Release --no-restore

      - name: Run Tests
        run: dotnet test MyWpfApp.sln --no-build --configuration Release --verbosity normal

      - name: Publish Artifacts (Optional)
        run: dotnet publish MyWpfApp.sln --configuration Release --output ./publish

      - name: Upload Build Artifacts (Optional)
        uses: actions/upload-artifact@v3
        with:
          name: published-app
          path: ./publish
